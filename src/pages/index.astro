---
import Layout from '../layouts/Layout.astro';
import Header from '../layouts/Header.astro';
import Card from '../components/Card.astro';
import Button from '../components/Button.contact.astro';
---

<Layout title="Welcome to Astro.">
	<main class="w-full min-h-screen bg-gradient-to-b from-slate-700 to-slate-900 flex flex-col">
		<Header/>
		<section class="grid grid-cols-2 items-start py-8 mt-20 w-full md:w-1/2 mx-auto">
			<img src="/favicon.svg" alt="icon" class="w-20 mx-auto">
			<div class=" mx-auto">
				<h1 id="textAnimate" class="animation-writter overflow-hidden text-nowrap text-left text-4xl text-transparent font-semibold bg-clip-text bg-gradient-to-b from-purple-700 to-blue-200">
					Fullstack Developer&nbsp;
					<!-- <span id="w">W</span>
					<span id="e">e</span>
					<span id="b">b</span>
					<span id="space">&nbsp;</span>

					<span id="d">D</span>
					<span id="e">e</span>
					<span id="v">v</span>
					<span id="pointer">|</span> -->
				</h1>
				<p class="italic font-semibold text-slate-200 text-2xl inline-block">Matías Sandoval</p>
				<p class="italic font-semibold text-slate-200 text-md inline-block ml-2">Ingeniero en Informática</p>
				<div class="flex w-full gap-4 py-4">
					<Button>Contáctame</Button>
					<Button>GitHub</Button>
					<Button>LinkedIn</Button>
				</div>
			</div>
		</section>
	</main>
</Layout>

<script>
	import {animate,timeline} from 'motion';

	const w = document.getElementById('w');
	const e = document.getElementById('e');

	// if (w) animate(w, { opacity: 1 }, { easing: "ease-in" });
	// if (e) animate(e, { opacity: 1 }, { easing: "ease-in" });

	const sequence = [
		["#w", { opacity: 1 }, { duration: 0.1 }],
		["#e", { opacity: 1 }, { duration: 0.1 }],
		["#b", { opacity: 1 }, { duration: 0.1 } ],
		["#space", { opacity: 1 }, { duration: 0.1 } ],
		["#d", { opacity: 1 }, { duration: 0.1 } ],
		["#e", { opacity: 1 }, { duration: 0.1 } ],
		["#v", { opacity: 1 }, { duration: 0.1 } ],
		["#pointer", { transform: "ease-in" }, { duration: 0.5, at: 0.02 } ],
	];

	// timeline(sequence);
</script>

<style>
	#textAnimate{
    /* position: relative; */
    /* top: 50%;   */
    /* width: 10em; */
    /* margin: 0 auto; */
    border-right: 2px solid rgba(255,255,255,.75);
    /* font-size: 180%; */
    /* text-align: center;
    white-space: nowrap;
    overflow: hidden; */
    /* transform: translateY(-50%);     */
}

/* Animation */
.animation-writter{
  animation: typewriter 2s steps(30) 0.5s 1 normal both,
             blinkTextCursor 550ms steps(44) infinite normal;
}
@keyframes typewriter{
  from{width: 0;}
  to{width: 100%}
}
@keyframes blinkTextCursor{
  from{border-right-color: rgba(255,255,255,.75);}
  to{border-right-color: transparent;}
}
</style>
